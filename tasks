xstep

let i = x.indexOf(computed.x[0]);
let firstStep = ((i + 1) % 6) * xstep;

Даты не должны изменяться. 
Нужно создать массив из всех дат по всему иксу.



let xStep = size / newMas.length;
let i = x.indexOf(computed.x[0]);